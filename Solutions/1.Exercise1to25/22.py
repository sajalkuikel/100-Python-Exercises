from pprint import pprint

d = {"a":list(range(1, 11)), "b":list(range(11, 21)), "c":list(range(21, 31))}
pprint(d)
pprint
dictionary= {"a" : list(range(5,15)), "b": list(range(11,21)), "c": list(range(21, 31)) }
pprint(dictionary)

#We're using ranges here to construct the lists. We're also using the built-in Python pprint
# module which is used to print out well formatted views of datatypes in Python.